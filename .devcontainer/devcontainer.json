{
  "name": "Phlow Dev",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "postCreateCommand": "set -x && apt update && apt install -y curl && curl -fsSL https://raw.githubusercontent.com/phlowdotdev/phlow/main/scripts/install-phlow.sh | { bash || true; } && echo '✅ Phlow instalado com sucesso!'",
  "customizations": {
    "vscode": {
      "extensions": [
        "cschleiden.vscode-github-actions"
      ]
    }
  }
}
